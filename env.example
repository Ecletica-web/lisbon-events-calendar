# Copy this file to .env.local and add your Google Sheets CSV URL
# To get the CSV URL from Google Sheets:
# 1. Open your Google Sheet
# 2. File > Share > Publish to web
# 3. Select the sheet (e.g., "events_clean")
# 4. Choose "CSV" format
# 5. Copy the URL and paste it below

NEXT_PUBLIC_EVENTS_CSV_URL=your_google_sheets_csv_url_here

# Optional: Venues CSV from Google Sheets (if not set, uses built-in canonical venues)
# NEXT_PUBLIC_VENUES_CSV_URL=

# Optional: Event Tags CSV - single column "tag". If set, only tags in this list are kept (max 5 per event).
# NEXT_PUBLIC_EVENT_TAGS_CSV_URL=

# Optional: Venue Tags CSV - single column "tag". If set, only tags in venue_tags from this list are kept.
# NEXT_PUBLIC_VENUE_TAGS_CSV_URL=

# Optional: Promoters CSV. Columns: promoter_id,name,slug,instagram_handle,website_url,description_short,primary_image_url,is_active
# NEXT_PUBLIC_PROMOTERS_CSV_URL=

# Feature flags (all default OFF)
# Set to 'true' to enable profile/auth features
NEXT_PUBLIC_ENABLE_PROFILE=false
# Personas: filter presets (taste profiles)
NEXT_PUBLIC_ENABLE_PERSONAS=false
# Shared views: public /v/[slug] and /p/[slug] routes
NEXT_PUBLIC_ENABLE_SHARED_VIEWS=false

# NextAuth Configuration
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Google OAuth (Optional - for Google login)
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth (Optional - for Facebook login)
# Get credentials from: https://developers.facebook.com/
FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret
